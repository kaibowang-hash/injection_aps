# Phase 0 隔离环境验证证据（2026-08-14）

## 1. 结论

Phase 0 的隔离数据库、迁移、Fixture、权限、Flag-Off、Legacy 指纹、前端保护和 OR-Tools 兼容性门禁均已完成。`jce.1` 未执行 migrate、Patch、Fixture、run-tests、build、restart、clear-cache 或任何写操作；本证据不构成生产发布授权。

## 2. 隔离边界

- Bench：`/home/ubuntu/frappe-bench/isolated/aps-v2-bench`
- 只读基线站点：`aps-opt-test.localhost`，数据库 `aps_v2_t_260814`
- 可写 Fixture 站点：`aps-opt-fixture.localhost`，数据库 `aps_v2_fixture_260814`
- MariaDB：loopback `13306`；Redis：`13100/11100`；测试 Web：`8200`
- 两站点均设置 `pause_scheduler=1`、`mute_emails=1`、`disable_async=1` 和 `aps_v2_isolated_environment=1`；Fixture 站点另有 `aps_v2_fixture_environment=1`。
- 所有变更工具先执行硬保护：拒绝 `jce.1`、3306、非 `aps_v2_*` 数据库、非 loopback 数据库和未关闭副作用的站点。

## 3. Migration 与默认配置

- 在生产数据隔离副本完成两轮 migrate；第二轮成功且没有待执行 Phase 0 Patch。
- `initialize_aps_v2_phase0_defaults` 直接重跑两次成功；Patch Log 仅一条。
- 首轮验证发现“数据库中数值 0 被误当成已有有效配置”的缺陷，Patch 已改为只保留正数；修复后再次迁移和直接重跑均稳定。
- 最终配置：四个 V2 Boolean Flag 均为 `0`，`solver_engine=Legacy`，交付容差 `3` 天，执行陈旧阈值 `30` 分钟，Full/Shift Solver 时限为 `120/30` 秒。
- Legacy planning horizon 未由 Phase 0 Patch 修改。

## 4. Legacy 基线和 Flag-Off

### 4.1 生产数据只读快照

- 隔离基线副本中 8 个现有 APS Planning Run 均连续捕获两次且内容指纹一致。
- 证据目录：`/home/ubuntu/frappe-bench/isolated/aps-v2-bench/evidence/phase_00/legacy_baseline`
- 总耗时：`0.920430s`；捕获模式：`READ_ONLY`。

### 4.2 Legacy 行为 Fixture

- Legacy Fixture 两轮重复指纹：`17d8ef72a76739da2e0c848dff4b84befe1f6830412a64d1fba457e59ea0c5fe`。
- Patch、第二轮 migrate、全量测试和 V2 Fixture 完整清理后，Legacy 指纹均恢复并保持同一值。
- V2 Fixture 存在时 Legacy 公司级汇总指纹会按预期改变；执行 `remove_masters=True` 的前缀完整清理后恢复原值，证明清理没有残留，也避免把测试范围变化误判为算法回归。
- 最终证据：`.../evidence/phase_00/legacy_post_final_full_tests`。

### 4.3 实际权限与只读性

- `Manufacturing Manager` 测试用户可调用 `get_v2_capabilities`、`capture_legacy_baseline`、`get_legacy_v2_comparison`。
- 仅有 `Employee` 角色的测试用户调用三项 API 均得到 `PermissionError`。
- 调用前后 APS Run/Result/Segment、Proposal、Release、Work Order、Work Order Scheduling、Delivery Plan 数量完全一致。
- API 返回 `mode=Legacy`、`formal_v2_writes_enabled=false`、Comparison `Not Available`。
- 最终证据：`.../evidence/phase_00/permission_flag_off_final/manifest.json`。

## 5. 11 个核心 Fixture

- 11 个场景已定义为“输入事实 + 预期不变量”，Phase 0 不伪造后续 Phase 才能产生的 Demand Identity、Commitment、Campaign 或正式 V2 输出。
- 每轮创建 12 个排期版本、3 个 Formal Run 基线、3 个对应 Result 和 4 条模具资源规则；所有测试名称均使用 `APS-V2-FIXTURE-` 前缀。
- 创建/清理/再创建的两轮指纹一致：`385c0a298529e377aab24dc341ff6818e9bd526eb3dcf093d4b7b047ac239b8f`。
- 完整清理仅删除前缀记录；验证后已重新建立 Fixture，供下一 Phase 使用。
- 最终证据：`.../evidence/phase_00/v2_input_fixtures_final/manifest.json`。

## 6. OR-Tools 兼容性

- 隔离环境：Python `3.10.12`、protobuf `3.20.3`。
- OR-Tools `9.15` 到 `9.5` 的依赖解析均要求 protobuf 4/5/6/7，与当前 `google-api-core` 的 `protobuf<4` 约束冲突。
- 选定内嵌兼容版本 `ortools==9.4.1874`；安装后 `pip check` 无损坏依赖，Frappe 和 `google.api_core` 均可导入。
- CP-SAT 单变量模型返回 `OPTIMAL`、目标值 `10`；protobuf 仍为 `3.20.3`。
- Phase 0 只在隔离环境安装，不写入生产依赖，不启用 Solver；若未来升级 OR-Tools，必须使用依赖隔离的求解器进程。
- 证据：`.../evidence/phase_00/ortools_compatibility/manifest.json`。

## 7. 前端定制保护

- recurring `after_migrate` 不再写 Workspace、Custom HTML Block、Custom Field、Property Setter、角色、默认设置、Seed 或缓存。
- Custom HTML Block 和 Workspace 均为 create-if-missing；已有内容不更新；无效 Workspace JSON 不保存。
- Custom Field 默认 `update=False`，Item `field_order` 只有显式 opt-in 才允许重排。
- 全栈首次 migrate 的原始比较记录 15 个元数据哈希变化；Injection APS 的 Workspace/Page/Custom HTML/Property Setter 无内容变化。5 个 Item Custom Field 仅由 Frappe 重算 `idx`，其 `insert_after`、label、type、hidden 和其他定义均未改变。
- 第一次 migrate 后到第二次 migrate 的语义比较为 `semantic_change_count=0`，证明 recurring migrate 稳定。
- 隔离证据：`.../evidence/frontend-*.json`；新增静态/Mock 门禁全部通过。

## 8. 测试结果

### 8.1 Phase 0 定向门禁

- 最终 25 项静态/Mock/契约/翻译/安装顺序测试全部通过。
- 11 场景、权限/Flag-Off、Legacy、OR-Tools 和迁移门禁全部通过。

### 8.2 当前工作树全量基线

- 命令在隔离站点以低 CPU/I/O 优先级运行。
- 最终结果：`600 tests`，`77.699s`，`3 failures`，`8 errors`。
- Phase 0 新增字段最初引起 1 个中文翻译失败，已补齐并从最终失败列表移除。
- 当前基线中的 11 项未在 Phase 0 扩大范围修复：
  - 2 个 Capacity Balance transaction consistency errors；
  - 6 个 Change Engine snapshot-staleness errors；
  - 1 个 Legacy execution FIFO 期望 `60/40`、实际 `65/35`；
  - 2 个 Phase 6 quantity audit failures。
- 这些失败作为当前工作树基线保留；Phase 1 及后续不得把它们误报为新回归，也不得在无独立分析时顺手修改 Legacy 行为。

## 9. Exit Criteria

| 门禁 | 结果 |
|---|---|
| V2 开关默认关闭、Solver 默认 Legacy | PASS |
| Legacy baseline 可重复生成并归档 | PASS |
| 11 个核心 Fixture 可重复创建和前缀清理 | PASS |
| Patch/migrate 幂等 | PASS |
| 实际角色权限和 Flag-Off 无写入 | PASS |
| 既有前端资源不被 recurring migrate 覆盖 | PASS |
| OR-Tools 兼容性有明确版本和隔离升级路径 | PASS |
| 状态、追踪矩阵、ADR 和 Handoff 更新 | PASS |

Phase 0 可以标记 `COMPLETE`；Phase 1 仍为 `NOT_STARTED`，不得由本证据自动启用或发布任何 V2 行为。

# Phase 0 静态与无站点测试证据（2026-08-14）

> 历史子集：隔离数据库验证已经完成，最终结论见 [2026-08-14 isolated evidence](./2026-08-14_isolated.md)。

## 安全范围

- 未对 `jce.1` 或 `jce-test.1` 执行任何写操作。
- 未运行 migrate、Patch、bench run-tests、build、restart、clear-cache、scheduler 或 Fixture。
- 仅修改 `apps/injection_aps` 工作树，并执行源码/JSON检查、Python 编译和无站点 mock 单元测试。

## 开始前 Baseline

命令：

```bash
/home/ubuntu/frappe-bench/env/bin/python -m unittest injection_aps.tests.test_import_transaction_guards
```

结果：83 tests，66 通过、17 errors。错误均发生在未初始化 Frappe site context 的测试路径，包括 `frappe.local.flags`、缓存、时区和日志路径未绑定；该命令未连接站点，也未写数据库。此结果作为执行环境 Baseline，不将其误报为本次代码回归。

## 本次通过证据

命令：

```bash
/home/ubuntu/frappe-bench/env/bin/python -m unittest \
  injection_aps.tests.test_frontend_customization_safety \
  injection_aps.tests.test_v2_phase0_contracts \
  injection_aps.tests.test_import_transaction_guards.TestImportAndTransactionGuards.test_roles_are_created_before_workspace_on_install \
  injection_aps.tests.test_import_transaction_guards.TestImportAndTransactionGuards.test_item_field_order_preserves_site_layout_and_moves_aps_fields_to_manufacturing
```

结果：16 tests，全部通过（14 个新增安全/Phase 0 测试，加 2 个既有安装与布局兼容测试）。

覆盖：

- 既有 Custom HTML Block 不覆盖。
- Workspace JSON 损坏时不保存、不重建为空。
- `after_migrate` 不再隐式写 Custom Field、角色、权限、Workspace、seed/default settings 或 Item 布局；这些变化必须进入可审查 Patch/显式操作。
- V2 Flag 缺字段时 fail closed，所有正式 V2 写入关闭。
- Legacy snapshot 指纹对行顺序和采集时间稳定。
- V2 comparison 明确返回 Phase 0 Not Available。
- API 要求 APS read + Planning Run scope 权限。
- 11 个核心场景已经建立只读目录，未启用 Fixture 写入。

其他检查：

```bash
/home/ubuntu/frappe-bench/env/bin/python -m compileall -q <本次 Python 文件>
/home/ubuntu/frappe-bench/env/bin/python -m json.tool <APS Settings/Workspace/Page JSON>
git diff --check
```

结果：全部通过。

## OR-Tools

无站点检测结果：`found=False`、`version=not-installed`。未在生产 bench 安装任何依赖。兼容性验证延后到获批隔离环境，因此 Phase 0 不能标记 Complete。

## 尚未执行

- Schema/Patch 重跑。
- 生产脱敏副本的 Legacy 全量回归。
- 11 个场景的数据库 Fixture 创建/清理。
- V1 快照的实际规模捕获与性能数据。
- UI 和权限集成测试。
- OR-Tools 安装/导入兼容性。
